{
  "name": "@autoapply/extension",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "@types/chrome": "^0.0.268"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-popup.js",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run"
  }
}
